describe("Secrets Manager", () => {
  describe("createSecret", () => {
    test("aws sdk createSecret call is correct");
  });

  describe("getSecretValue", () => {
    test("aws sdk getSecretValue call is correct");
    test("it create secret if secret doesn't exist");
  });
});

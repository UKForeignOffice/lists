{% from "button/macro.njk" import govukButton %}
{% extends "./dashboard.njk" %}

{% block aside %}
{% endblock %}

{% block dashboard %}

  <form class="form" action="{{ dashboardRoutes.listsEditAnnualReviewDate.replace(':listId', list.id) }}" method="post" required>
    <input type="hidden" name="_csrf" id="_csrf" value="{{ csrfToken }}" />
    <input type="hidden" name="newAnnualReveiwDate" value="{{ newAnnualReveiwDate }}" />

    <h2 class="govuk-heading-m" style="color: #b1b4b6;">{{list.country.name}}</h2>

    <h1 class="govuk-heading-l">
      Confirm new annual review start date
    </h1>

    <p class="govuk-body">You are about to change the next funeral directors list annual review date to<br /> <strong>{{ newannualReviewDateFormatted }}.</strong></p>

    <p class="govuk-body">Confirming this will:</p>
    <ul class="govuk-list govuk-list--bullet">
        <li>change the annual review date to the new date</li>
        <li>email the new date to all consular users who have access to this list </li>
    </ul>

    <div class="govuk-button-group">

      <button class="govuk-button govuk-!-margin-bottom-2" data-module="govuk-button" name="action" value="submitNewDate">
        Continue
      </button>

      <a class="govuk-button govuk-button--secondary govuk-!-margin-bottom-2" href="{{ dashboardRoutes.listsEdit.replace(':listId', list.id) }}">Cancel</a>
    </div>

  </form>

{% endblock %}
